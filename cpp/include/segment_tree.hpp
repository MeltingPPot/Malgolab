#ifndef SEGMENT_TREE_HPP   // 头文件保护，防止重复包含
#define SEGMENT_TREE_HPP

#include <vector>
#include <functional>

namespace Malgolab{
    class{
        public:
            #define l
        private:
            int n;
            void push_up(int pos){
                t[pos].dat=
            }
            void build(int )
    }

} // namespace algolib
#endif